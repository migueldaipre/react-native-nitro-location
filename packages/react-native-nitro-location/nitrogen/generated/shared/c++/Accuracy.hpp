///
/// Accuracy.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2024 Marc Rousavy @ Margelo
///

#pragma once

#include <cmath>
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::nitro_location {

  /**
   * An enum which can be represented as a JavaScript enum (Accuracy).
   */
  enum class Accuracy {
    LOWEST      SWIFT_NAME(lowest) = 1,
    LOW      SWIFT_NAME(low) = 2,
    BALANCED      SWIFT_NAME(balanced) = 3,
    HIGH      SWIFT_NAME(high) = 4,
    HIGHEST      SWIFT_NAME(highest) = 5,
    BESTFORNAVIGATION      SWIFT_NAME(bestfornavigation) = 6,
  } CLOSED_ENUM;

} // namespace margelo::nitro::nitro_location

namespace margelo::nitro {

  using namespace margelo::nitro::nitro_location;

  // C++ Accuracy <> JS Accuracy (enum)
  template <>
  struct JSIConverter<Accuracy> {
    static inline Accuracy fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      int enumValue = JSIConverter<int>::fromJSI(runtime, arg);
      return static_cast<Accuracy>(enumValue);
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, Accuracy arg) {
      int enumValue = static_cast<int>(arg);
      return JSIConverter<int>::toJSI(runtime, enumValue);
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isNumber()) {
        return false;
      }
      double integer;
      double fraction = modf(value.getNumber(), &integer);
      if (fraction != 0.0) {
        // It is some kind of floating point number - our enums are ints.
        return false;
      }
      // Check if we are within the bounds of the enum.
      return integer >= 0 && integer <= 5;
    }
  };

} // namespace margelo::nitro
