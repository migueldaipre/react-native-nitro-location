///
/// HybridLocationResultSpecCxx.swift
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2024 Marc Rousavy @ Margelo
///

import Foundation
import NitroModules

/**
 * A class implementation that bridges HybridLocationResultSpec over to C++.
 * In C++, we cannot use Swift protocols - so we need to wrap it in a class to make it strongly defined.
 *
 * Also, some Swift types need to be bridged with special handling:
 * - Enums need to be wrapped in Structs, otherwise they cannot be accessed bi-directionally (Swift bug: https://github.com/swiftlang/swift/issues/75330)
 * - Other HybridObjects need to be wrapped/unwrapped from the Swift TCxx wrapper
 * - Throwing methods need to be wrapped with a Result<T, Error> type, as exceptions cannot be propagated to C++
 */
public final class HybridLocationResultSpecCxx {
  /**
   * The Swift <> C++ bridge's namespace (`margelo::nitro::nitro_location::bridge::swift`)
   * from `NitroLocation-Swift-Cxx-Bridge.hpp`.
   * This contains specialized C++ templates, and C++ helper functions that can be accessed from Swift.
   */
  public typealias bridge = margelo.nitro.nitro_location.bridge.swift

  /**
   * Holds an instance of the `HybridLocationResultSpec` Swift protocol.
   */
  private(set) var implementation: HybridLocationResultSpec

  /**
   * Create a new `HybridLocationResultSpecCxx` that wraps the given `HybridLocationResultSpec`.
   * All properties and methods bridge to C++ types.
   */
  public init(_ implementation: HybridLocationResultSpec) {
    self.implementation = implementation
  }

  /**
   * Contains a (weak) reference to the C++ HybridObject to cache it.
   */
  public var hybridContext: margelo.nitro.HybridContext {
    get {
      return self.implementation.hybridContext
    }
    set {
      self.implementation.hybridContext = newValue
    }
  }

  /**
   * Get the memory size of the Swift class (plus size of any other allocations)
   * so the JS VM can properly track it and garbage-collect the JS object if needed.
   */
  public var memorySize: Int {
    return self.implementation.memorySize
  }

  // Properties
  public var coords: HybridLocationCoordsSpecCxx {
    @inline(__always)
    get {
      return self.implementation.coords.createCxxBridge()
    }
  }
  
  public var timestamp: Double {
    @inline(__always)
    get {
      return self.implementation.timestamp
    }
  }
  
  public var mocked: bridge.std__optional_bool_ {
    @inline(__always)
    get {
      return { () -> bridge.std__optional_bool_ in
        if let actualValue = self.implementation.mocked {
          return bridge.create_std__optional_bool_(actualValue)
        } else {
          return .init()
        }
      }()
    }
  }

  // Methods
  
}
